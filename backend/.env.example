# ─────────────────────────────────────────
#  ZakatAid Backend – Environment Variables
# ─────────────────────────────────────────

# Server
PORT=5000
NODE_ENV=development

# Database – MongoDB (local or Atlas URI)
MONGODB_URI=mongodb://localhost:27017/zakataid

# JWT Secrets
JWT_SECRET=your_super_secret_jwt_key_change_in_production
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your_refresh_secret_key_change_in_production
JWT_REFRESH_EXPIRES_IN=30d

# Email (Nodemailer / SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_gmail_app_password
EMAIL_FROM="ZakatAid <noreply@zakataid.com>"

# Frontend Origin (for CORS) - can be comma-separated for multiple origins
CLIENT_URL=http://localhost:3000,http://localhost:5173,https://your-app.netlify.app

# OTP Settings
OTP_EXPIRES_MINUTES=10
