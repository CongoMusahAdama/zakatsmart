import ComingSoon from "@/components/dashboard/ComingSoon";

export default function ImpactPage() {
    return (
        <ComingSoon
            title="Impact Tracker"
            subtitle="Track exactly where your Zakat goes and the real impact it creates in your community — in real time."
            arabicLabel="الأثر"
            iconName="history"
        />
    );
}
